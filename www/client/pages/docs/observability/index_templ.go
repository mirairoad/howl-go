// Code generated by templ - DO NOT EDIT.

// templ: version: v0.3.1020
// Code generated by core/cmd/mddocs from the Markdown source. DO NOT EDIT.

package observability

//lint:file-ignore SA4006 This context is only used if a nested component is present.

import "github.com/a-h/templ"
import templruntime "github.com/a-h/templ/runtime"

func Head() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var1 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var1 == nil {
			templ_7745c5c3_Var1 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 1, "<title>Observability — howl-go</title><meta name=\"description\" content=\"Traces of every request, page render, endpoint call, guard and document operation, and the metrics that fall out of them, exported over OTLP. Nothing in the …\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

func Page() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var2 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var2 == nil {
			templ_7745c5c3_Var2 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 2, "<article class=\"prose\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templ.Raw(body).Render(ctx, templ_7745c5c3_Buffer)
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 3, "</article>")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

const body = "<h1>Observability</h1>\n<p>Traces of every request, page render, endpoint call, guard and document operation, and the metrics that fall out of them, exported over OTLP. Nothing in the application opens a span for any of it.</p>\n<h2>Two parts</h2>\n<p><code>core/observe</code> is the framework's vocabulary for &quot;something is happening&quot;: a <code>Span</code> with three methods — <code>Set</code>, <code>Event</code>, <code>End</code> — and a <code>Tracer</code> that opens one. <code>App.render</code>, the endpoint pipeline, <code>mw.Cache</code>, <code>mw.RateLimit</code> and every <code>db.Service</code> operation call it. It imports nothing, and its zero state is a no-op: an application that never heard of tracing pays a nil check per operation.</p>\n<p><code>otel/</code> is a nested module with its own <code>go.mod</code>, like <code>db/pg</code> and <code>desktop</code>. It holds the OpenTelemetry SDK — some twenty modules — and implements the tracer. The framework's <code>go.mod</code> keeps its one dependency.</p>\n<h2>Setup</h2>\n<pre><code class=\"language-go\">shutdown, err := otel.Setup(ctx, otel.WithServiceName(&quot;pack&quot;))\ndefer shutdown(ctx)\n\na := app.New(app.Config{\n\tUse: []mw.Middleware{\n\t\totel.HTTP(),                                // first: the request span\n\t\tmw.RequestID,\n\t\tmw.LogWith(mw.LogOptions{Callers: true, Skip: mw.SkipNoise}),\n\t\tmw.Named(&quot;guard.session&quot;, guards.Session),  // a guard, as itself in the trace\n\t},\n})\napi.Register(mux, api.Config{OnError: otel.RecordError}, apis.FsApiRoutes()...)\nslog.SetDefault(slog.New(otel.SlogHandler(slog.Default().Handler())))\n\nlog.Fatal(a.Listen(otel.Routes(mux)))\n</code></pre>\n<p>The environment does the rest, and it is the standard one:</p>\n<pre><code>OTEL_EXPORTER_OTLP_ENDPOINT=&quot;http://10.19.96.7:4318&quot;\nOTEL_EXPORTER_OTLP_PROTOCOL=&quot;http/protobuf&quot;\nOTEL_RESOURCE_ATTRIBUTES=&quot;service.instance.id=prod,deployment.environment=prod&quot;\nOTEL_SERVICE_NAME=&quot;pack&quot;\n</code></pre>\n<p><code>OTEL_SERVICE_NAME</code> beats <code>WithServiceName</code>; a deployment renames a binary without rebuilding it. <code>OTEL_TRACES_SAMPLER</code>, <code>OTEL_METRIC_EXPORT_INTERVAL</code> and <code>OTEL_EXPORTER_OTLP_HEADERS</code> are honoured by the SDK. <code>OTEL_TRACES_EXPORTER=none</code> or <code>OTEL_METRICS_EXPORTER=none</code> turns one signal off.</p>\n<p>The module speaks <code>http/protobuf</code> only — the collector's <code>:4318</code> — and refuses any other <code>OTEL_EXPORTER_OTLP_PROTOCOL</code> at <code>Setup</code>. Exporting nothing while the process believes it is exporting is the one failure an observability library must not have, and the gRPC exporter's dependency tree is what the module exists not to carry.</p>\n<h2>What a trace looks like</h2>\n<pre><code>GET /dashboard/{id}                    the request; named by the route once the mux has matched\n├─ guard.session                       a middleware in mw.Named, to the point it handed over\n├─ howl.render /dashboard/{id}         the page and its layouts; howl.mode, howl.client, howl.bytes\n│  └─ db.get users                     a read the page made; collection, operation, backend\n└─ api Metrics                         an endpoint; howl.api.stage on failure\n   └─ db.find orders\n</code></pre>\n<p>The request span opens named by the method alone — the mux has not matched yet — and is renamed the moment the framework learns the route, in <code>App.render</code> or the endpoint pipeline. A static file or a 404 keeps the bare method, which is what the conventions say to do when there is no route.</p>\n<p>A guard wrapped in <code>mw.Named</code> gets a span that ends the moment it calls the next handler, so its own cost stands apart from the page behind it. One that answers instead — a redirect to sign-in — keeps the span to the end of its reply and records the status and the <code>Location</code>: the answer to &quot;why did the dashboard bounce this user&quot;.</p>\n<p>Events on the request span mark <code>cache.hit</code> / <code>cache.miss</code> — <code>mw.Cache</code>, <code>//howl:cache</code>, <code>Spec.Cache</code> and a document read's cache alike — and <code>ratelimit.refused</code>. A <code>//howl:cache</code> hit has no render span at all; the absence is the information. Every traced response carries <code>X-Trace-Id</code>, so the trace behind a screenshot of a network tab can be found.</p>\n<h2>Every method, and the handlers the framework does not route</h2>\n<p>A page route is registered <code>GET</code> — a page is a document — but nothing else here is GET-only. An endpoint is whatever its file name says: <code>logs.post.api.go</code> is a <code>POST</code>, and it traces exactly like a <code>GET</code>, with the same span, the same stages and the same metrics. <code>mw.RateLimit</code>, guards, and every <code>db</code> write — <code>create</code>, <code>patch</code>, <code>delete</code>, <code>patch_where</code>, <code>delete_where</code>, <code>restore</code> — are method-blind. <code>mw.Cache</code> sees only GET because GET is all it caches.</p>\n<p>The exception is a handler registered on the mux <strong>by hand</strong>: a form post to <code>server/actions</code>, a fragment endpoint answering an <code>&lt;li&gt;</code>, a webhook. Neither <code>App.render</code> nor the endpoint pipeline runs for it, so nothing in the framework knows its route, and the span is a bare <code>POST</code> with no <code>http.route</code> — every hand-written write path in the application collapsing into one unnamed series.</p>\n<pre><code class=\"language-go\">log.Fatal(a.Listen(otel.Routes(mux)))\n</code></pre>\n<p><code>otel.Routes</code> reads the pattern the mux matched and names the span after it: <code>POST /api/todos</code>, <code>DELETE /api/todos/{id}</code>. The path template, not the URL — one time series for a route, not one per id.</p>\n<p>It has to wrap the mux directly, with nothing in between. <code>ServeMux</code> sets <code>Request.Pattern</code> in place on the request it was handed, so a middleware that clones the request — <code>mw.RequestID</code>, or anything calling <code>WithContext</code> — hides the pattern from everything outside it. <code>a.Listen</code> applies <code>Use</code> around the handler you give it, which puts the request span outside and this inside, where it belongs.</p>\n<h2>The database</h2>\n<p>Every <code>db.Service</code> call is a span, and the backend call is a span inside it:</p>\n<pre><code>db.get users              the operation — cache, decoding, the wait\n└─ sql.find_one users     the backend call\n</code></pre>\n<p>The gap between the two is the service's own cost: validating, building keys, unmarshalling, or waiting on another caller's query. Without it a slow unmarshal and a slow database look identical.</p>\n<p>Reads (<code>get</code>, <code>get_many</code>, <code>find</code>, <code>one</code>, <code>count</code>), writes (<code>create</code>, <code>patch</code>, <code>delete</code>, <code>restore</code>, <code>patch_where</code>, <code>delete_where</code>) and maintenance (<code>report</code>, <code>drop_field</code>, <code>columns</code>, <code>drop_column</code>) all open one. Beyond the collection, backend and operation name, each carries what only that operation knows:</p>\n<table>\n<thead>\n<tr>\n<th>attribute</th>\n<th>what it answers</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>howl.db.rows</code></td>\n<td>how many documents came back — &quot;8&quot; and &quot;8000&quot; being the interesting difference</td>\n</tr>\n<tr>\n<td><code>howl.cache.hits</code> / <code>howl.cache.misses</code></td>\n<td>how many lookups hit. Counted per operation, not one event per lookup: <code>GetMany</code> of fifty ids is one span with two numbers</td>\n</tr>\n<tr>\n<td><code>howl.db.session</code></td>\n<td>the call is inside a transaction, so the cache was bypassed in both directions</td>\n</tr>\n<tr>\n<td><code>howl.db.attempts</code></td>\n<td>how many times a patch retried its optimistic lock; reaching 3 is <code>ErrConflict</code> about to happen</td>\n</tr>\n<tr>\n<td><code>howl.db.coalesced</code></td>\n<td>this read waited on another caller's identical query and issued none of its own — a slow span with nothing under it, explained</td>\n</tr>\n<tr>\n<td><code>howl.db.bulk</code></td>\n<td><code>native</code> when the backend wrote the set in one statement, <code>fallback</code> when the service walked the rows one at a time</td>\n</tr>\n</tbody>\n</table>\n<p>The document id and the row count stay attributes, never metric labels.</p>\n<h2>The query, recorded and redacted</h2>\n<p><code>db.query.text</code> carries the filter and <code>howl.db.set</code> what an update wrote — the two things that answer &quot;which documents did that touch, and what did it do to them&quot;. A <code>DeleteWhere</code> that hit more rows than anyone expected is unreadable without them.</p>\n<p>The shape is always recorded: field names, operators, how many elements an <code>in</code> clause had. Those are the schema, not the data. What <code>db.Options.Trace</code> decides is the values.</p>\n<table>\n<thead>\n<tr>\n<th>mode</th>\n<th>values</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>observe.Safe</code> — the zero value</td>\n<td>numbers, booleans, null, and strings that are identifiers: a UUID (howl's own ids are UUIDv7), a ULID, a hex digest, digits. Everything else becomes <code>&quot;?&quot;</code></td>\n</tr>\n<tr>\n<td><code>observe.Full</code></td>\n<td>every value, except the two rules below. For a development machine, or a service holding no personal data</td>\n</tr>\n<tr>\n<td><code>observe.Off</code></td>\n<td>nothing — not the values, not the field names</td>\n</tr>\n</tbody>\n</table>\n<p>Two rules hold in <strong>every</strong> mode, <code>Full</code> included:</p>\n<ol>\n<li><strong>A sensitive field name redacts its whole subtree.</strong> <code>password</code>, <code>api_key</code>, <code>email</code>, <code>phone</code>, <code>dob</code>, <code>card</code>, <code>iban</code>, <code>session</code> and the rest, matched by word rather than by substring — so <code>api_key</code> and <code>key</code> are sensitive while <code>keyword</code> and <code>monkey</code> are not. A word break is punctuation <em>or</em> a case change, because a JSON document is as likely to say <code>userEmail</code> as <code>user_email</code>; splitting only on punctuation made every camelCase spelling ordinary, and <code>phoneNumber</code> holding digits is a value Safe would otherwise have kept, digits being how an id looks.</li>\n<li><strong>Anything that looks like an email address is never recorded</strong>, wherever it appears: under an innocent field name, nested inside an <code>$or</code>, inside an array, even in key position. The test for it asserts exactly that, in both modes, at every depth — because this is the one piece of personal data that is recognisable without knowing the schema, and the one that turns a trace into a mailing list.</li>\n</ol>\n<pre><code>{&quot;limit&quot;:20,&quot;sort&quot;:{&quot;created&quot;:-1},&quot;where&quot;:{&quot;$and&quot;:[{&quot;email&quot;:{&quot;$eq&quot;:&quot;?&quot;}},\n {&quot;org_id&quot;:{&quot;$eq&quot;:&quot;0193a5c2-5f4e-7b3a-9c1d-2e6f8a0b4c7d&quot;}}],&quot;meta.deleted_at&quot;:null}}\n</code></pre>\n<p>Three details that make that readable. The id is kept by name in <code>howl.db.id</code>, because an id is how the document is found again — in the database and in the next trace. Sort and projection fields go in <strong>key</strong> position (<code>{&quot;created&quot;:-1}</code>), since a key is schema and a string value is data; written as <code>[&quot;-created&quot;]</code> they would render as <code>[&quot;?&quot;]</code> and say nothing. And an <code>in</code> clause over five hundred ids renders eight of them followed by <code>&quot;+492 more&quot;</code> — the count being the difference between two queries with the same shape. Output is capped at 1 KiB, depth at 6.</p>\n<p>An RFC 3339 <strong>date-time</strong> counts as an identifier, so a range query stays legible under Safe:</p>\n<pre><code>{&quot;meta.created_at&quot;:{&quot;$lt&quot;:&quot;2026-01-02T03:04:05Z&quot;}}\n</code></pre>\n<p>That is the difference between knowing a bulk delete swept by date and knowing what it deleted — which is the question being asked the moment somebody notices the row count. A moment names no one.</p>\n<p>A bare <strong>date</strong> does not count: <code>1985-03-12</code> is how a date of birth is written, and <code>2026-01-02T03:04:05Z</code> is not how anybody writes one by hand. A DOB stored as a <code>time.Time</code> does marshal to the second form, so the field name is what stops it — <code>dob</code>, <code>birth</code>, <code>birthdate</code>, <code>birthday</code> and <code>born</code> are denied in every spelling, <code>dateOfBirth</code> and <code>date_of_birth</code> alike.</p>\n<p>Endpoints do the same for what the caller sent — <code>howl.api.query</code> and <code>howl.api.body</code>, under <code>api.Config.Trace</code> — recorded <em>before</em> <code>Validate</code> runs, because a call refused for being wrong is exactly the one whose arguments you want to see.</p>\n<p><strong>Nothing is rendered unless a tracer is installed.</strong> Measured on <code>db.Find</code> against the in-memory backend: untraced is byte-identical to <code>Trace: observe.Off</code> — 16.1 µs and 50 allocations — while with a tracer attached it is 27.2 µs, of which rendering the filter is 2.3 µs and 22 allocations. Against a backend that talks to a real database over a socket, that is noise.</p>\n<p><code>One</code> gets its own span with the <code>Find</code> it delegates to nested inside, because a trace should say which of the two the caller wrote, and &quot;not found&quot; is <code>One</code>'s answer rather than <code>Find</code>'s.</p>\n<h2>The browser's half</h2>\n<p>A navigation starts its trace in the browser, so a click and the database query behind it end up in one trace rather than two.</p>\n<p><code>app.js</code> mints a W3C <code>traceparent</code> per navigation — sixteen random bytes and eight, from <code>crypto.getRandomValues</code>, with no SDK to load first — and sends it on everything that navigation causes: the fragment fetch, the route's <code>//howl:data</code> endpoint, and any request the page makes from Go, since <code>core/dom</code> reads <code>howl.traceparent()</code> and the generated API client goes through it. The server's propagator joins that trace, so the request span is already a child of the navigation before the navigation has even been reported.</p>\n<p>What the server cannot see is the rest: how long the swap took, and the navigations that never reached it at all.</p>\n<pre><code class=\"language-go\">mux.Handle(&quot;POST /api/telemetry/navigations&quot;, otel.Navigations())\n\na := app.New(app.Config{Telemetry: &quot;/api/telemetry/navigations&quot;, ...})\nlog.Fatal(a.Listen(otel.Routes(mux)))\n</code></pre>\n<p><code>Config.Telemetry</code> is published in the <code>howl-client</code> JSON the shell embeds, and nothing reports unless it is set — the same shape the dev client's reload endpoint uses. <code>app.js</code> batches rows for five seconds and flushes on <code>pagehide</code> through <code>sendBeacon</code>, which survives the document being torn down; a browser that refuses the beacon gets a <code>keepalive</code> POST instead. Either way it is fire-and-forget, so a telemetry endpoint that is down cannot slow a page down.</p>\n<p><code>otel.Navigations</code> creates each span with <strong>the browser's own span id</strong> rather than a fresh one. That is the whole trick: the request the server recorded arrived parented to that id, so the navigation ends up containing it.</p>\n<pre><code>howl.navigate /dashboard/metrics    220 ms   howl.mode=fragment  howl.bytes=1834\n└─ GET /dashboard/metrics            18 ms\n   └─ howl.render /dashboard/metrics  3 ms\n      └─ db.find orders               9 ms\n</code></pre>\n<p>A <code>howl.mode=wasm</code> row has no child at all, by construction — the server was never contacted — and that row is the only evidence the framework's fastest path ever ran.</p>\n<p>Every navigation also dispatches <code>howl:navigated</code> on the document with <code>{path, mode, ms, bytes, traceId, spanId}</code>, whether or not telemetry is configured, so an application that ships its numbers somewhere else can listen for that instead.</p>\n<h2>Metrics</h2>\n<table>\n<thead>\n<tr>\n<th>metric</th>\n<th>labelled by</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>http.server.request.duration</code></td>\n<td>otelhttp's; <code>http.route</code> once known</td>\n</tr>\n<tr>\n<td><code>howl.render.duration</code></td>\n<td><code>howl.route</code>, <code>howl.mode</code></td>\n</tr>\n<tr>\n<td><code>howl.api.duration</code>, <code>howl.api.errors</code></td>\n<td><code>howl.endpoint</code>, and <code>howl.api.stage</code> on errors</td>\n</tr>\n<tr>\n<td><code>db.client.operation.duration</code></td>\n<td><code>db.system.name</code>, <code>db.collection.name</code>, <code>db.operation.name</code></td>\n</tr>\n<tr>\n<td><code>howl.db.storage.duration</code></td>\n<td>the same three — the backend's share of the operation above</td>\n</tr>\n<tr>\n<td><code>howl.cache</code></td>\n<td><code>howl.kind</code> (<code>http</code> or <code>db</code>), <code>result</code> (<code>hit</code> or <code>miss</code>), <code>db.collection.name</code></td>\n</tr>\n<tr>\n<td><code>howl.ratelimit.refused</code></td>\n<td>—</td>\n</tr>\n</tbody>\n</table>\n<p>Two cache outcomes have no operation to hang them on, and come from each service's own counters instead:</p>\n<pre><code class=\"language-go\">otel.WatchCache(users, orders, invoices)\n</code></pre>\n<p><code>howl.db.cache.bypassed</code> counts reads that could not consult the cache at all, because the shared version was unreadable — a broken <code>Versioner</code>, which otherwise looks exactly like a cache that is merely cold. <code>howl.db.cache.too_large</code> counts results returned but never stored, being over <code>Cache.MaxEntryBytes</code> — the unbounded <code>Find</code> that will never be cached however often it is asked for. Both are observable counters read at collection time, so they cost nothing per operation.</p>\n<p>Durations are in seconds. The metrics are derived from the spans at <code>End</code>: a span the framework opened says what it is in <code>howl.kind</code>, and the tracer turns its duration into the histogram for that kind. One set of hooks, and a trace and a metric cannot disagree about what happened. Labels are the handful of bounded attributes above — an id or a byte count goes on the span only, because a label with unbounded values is how a metrics backend runs out of memory.</p>\n<h2>Your own spans and logs</h2>\n<pre><code class=\"language-go\">ctx, span := otel.Start(ctx, &quot;import.csv&quot;)\nspan.Set(&quot;rows&quot;, n)\ndefer span.End(err)\n</code></pre>\n<p><code>otel.Start</code> is <code>observe.Start</code> re-exported, so an application imports one package for the framework's spans and its own. <code>otel.Tracer(name)</code> and <code>otel.Meter(name)</code> hand back the SDK's objects for anything three methods cannot say.</p>\n<p><code>otel.SlogHandler</code> wraps any <code>slog.Handler</code> — <code>console</code>'s included — and adds <code>trace_id</code> and <code>span_id</code> to every record logged with a request context. Two keys, and a log line in the backend joins its trace. Exporting the records themselves over OTLP is the application's decision; the module keeps them wherever they already go.</p>\n<h2>Rules</h2>\n<ul>\n<li><code>otel.HTTP()</code> goes first in <code>Use</code>. Everything after it is inside the request span.</li>\n<li>The mux goes in <code>otel.Routes</code>, or every hand-written handler traces as a bare method.</li>\n<li>Do not open spans for what is already traced: a request, a render, an endpoint, a db operation.</li>\n<li>A guard is <code>mw.Named(&quot;guard.&lt;name&gt;&quot;, guard)</code>, not a hand-opened span.</li>\n<li>Nothing under <code>client/</code> may import <code>core/observe</code>. A page compiles into the wasm build too, where a span has no exporter. <code>howl check</code> reports it.</li>\n</ul>\n"

var _ = templruntime.GeneratedTemplate
