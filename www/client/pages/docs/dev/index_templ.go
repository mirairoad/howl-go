// Code generated by templ - DO NOT EDIT.

// templ: version: v0.3.1020
// Code generated by core/cmd/mddocs from the Markdown source. DO NOT EDIT.

package dev

//lint:file-ignore SA4006 This context is only used if a nested component is present.

import "github.com/a-h/templ"
import templruntime "github.com/a-h/templ/runtime"

func Head() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var1 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var1 == nil {
			templ_7745c5c3_Var1 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 1, "<title>The dev server — howl-go</title><meta name=\"description\" content=\"go run github.com/mirairoad/howl-go/core/cmd/howl dev\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

func Page() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var2 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var2 == nil {
			templ_7745c5c3_Var2 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 2, "<article class=\"prose\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templ.Raw(body).Render(ctx, templ_7745c5c3_Buffer)
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 3, "</article>")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

const body = "<h1>The dev server</h1>\n<pre><code class=\"language-bash\">go run github.com/mirairoad/howl-go/core/cmd/howl dev\n</code></pre>\n<p>Watch the tree, regenerate the route table, run templ, rebuild, restart the binary, reload the browser. Replaces <code>go run .</code> and the four commands you were running before it.</p>\n<pre><code>10:15:45.538 INFO  howl dev    url=http://localhost:9000 watching=. pages=example.com/myapp/client/pages\n10:15:46.437 INFO  listening   url=http://127.0.0.1:49766 routes=8\n10:15:46.457 INFO  rebuilt     why=&quot;first build&quot; build=890.62ms total=918.54ms\n10:16:27.088 INFO  css         file=client/public/app.css\n10:16:30.649 INFO  rebuilt     why=client/pages/index.templ build=535.5ms total=564.18ms\n</code></pre>\n<h2>What happens on a change</h2>\n<table>\n<thead>\n<tr>\n<th>you change</th>\n<th>what runs</th>\n<th>browser</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>a <code>.css</code> file under <code>client/public/</code></td>\n<td>nothing</td>\n<td>stylesheet swapped in place</td>\n</tr>\n<tr>\n<td>anything else under <code>client/public/</code></td>\n<td>nothing</td>\n<td>reload</td>\n</tr>\n<tr>\n<td>a <code>.templ</code> under <code>client/pages/</code></td>\n<td>fsroutes → templ → build → restart</td>\n<td>reload</td>\n</tr>\n<tr>\n<td>any other <code>.templ</code></td>\n<td>templ → build → restart</td>\n<td>reload</td>\n</tr>\n<tr>\n<td>a <code>.go</code> file</td>\n<td>build → restart</td>\n<td>reload</td>\n</tr>\n<tr>\n<td>a <code>.md</code> file</td>\n<td>your <code>-pre</code> command, then all of the above</td>\n<td>reload</td>\n</tr>\n</tbody>\n</table>\n<p>Measured on the example app: <strong>~540 ms</strong> for a templ edit, <strong>~390 ms</strong> for a Go edit. The CSS path is instant, because nothing is rebuilt at all.</p>\n<p><strong>There is no HMR and this does not pretend otherwise.</strong> Go cannot hot-swap a linked binary — a new version of your code means a new process. What the dev server removes is the manual part, plus the two seconds you spend switching to the browser and pressing reload.</p>\n<h2>The port stays up</h2>\n<p>The browser talks to the dev server, never to your application directly. Your app is started on a private port and proxied.</p>\n<p>That buys three things worth having:</p>\n<ul>\n<li>The address in your address bar never stops answering. No connection-refused to reload past.</li>\n<li>A request that lands mid-rebuild <strong>waits</strong> for the new binary instead of failing. A page that takes 800 ms once beats an error page you have to dismiss.</li>\n<li>The live-reload connection survives the restart it is reporting, which is what makes the reload reliable rather than a race.</li>\n</ul>\n<h2>A failed build keeps the last good binary serving</h2>\n<p>The running process is not killed when a build fails. The page you are looking at keeps working, and the compiler error arrives in the browser as an overlay:</p>\n<pre><code>BUILD FAILED\n\nbuild: # example.com/myapp\n./main.go:166:22: syntax error: unexpected name is at end of statement\n\nfix it and save — this closes itself. esc to dismiss.\n</code></pre>\n<p>Fix it, save, and the overlay disappears as the page reloads. Reloading the browser onto a broken build would replace a working page with a blank one, and the error is already on screen.</p>\n<h2>Your application needs no dev-mode code</h2>\n<p>The dev server configures the app through three environment variables, which <code>core/app</code> reads in <code>app.New</code>:</p>\n<table>\n<thead>\n<tr>\n<th>variable</th>\n<th>effect</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>HOWL_ADDR</code></td>\n<td>the port the app listens on — the dev server picks a free one</td>\n</tr>\n<tr>\n<td><code>HOWL_PUBLIC_DIR</code></td>\n<td>serve <code>/static/</code> from this directory instead of the embedded copy</td>\n</tr>\n<tr>\n<td><code>HOWL_DEV</code></td>\n<td>publish the live-reload endpoint to the browser</td>\n</tr>\n</tbody>\n</table>\n<p><code>HOWL_PUBLIC_DIR</code> is the one that makes CSS instant: without it your stylesheet is inside the binary via <code>//go:embed</code>, so changing it would mean a rebuild. It also switches the static handler into re-read-every-request mode, so nothing is cached from a previous version of the file.</p>\n<p>None of them are set unless the dev server sets them, and <code>Config.PublicDir</code> / <code>Config.Addr</code> do the same thing in code if you would rather be explicit.</p>\n<h2>The client half — one message, two jobs</h2>\n<p><code>app.js</code> loads the dev client only when the shell publishes a <code>live</code> endpoint:</p>\n<pre><code class=\"language-js\">if (CONFIG.live) import(CONFIG.live + &quot;.js&quot;)\n</code></pre>\n<p>The dev client is served by the dev server at <strong><code>/_howl/alive.js</code></strong>, not embedded in the framework, so a production build ships one dead <code>if</code> rather than a kilobyte of code it will never run. <code>router.ClientConfig(ctx)</code> fills the field in — no change to your shell.</p>\n<p>The stream at <strong><code>/_howl/alive</code></strong> carries a <strong>monotonic revision</strong>, and the same message both greets a new connection and reloads an existing one:</p>\n<pre><code>event: alive\ndata: 1786501533683\n</code></pre>\n<p>The client remembers the first number it sees and reloads on anything higher. This is the pattern howl (TypeScript) uses on its own <code>/_howl/alive</code> socket, for a reason worth stating plainly:</p>\n<blockquote>\n<p><strong>A &quot;reload now&quot; event cannot survive a disconnection.</strong> An event fired while the browser was not listening is simply gone. A laptop that slept through a rebuild, a tab whose connection dropped, or a page left open across a restart of the dev server itself would all sit there stale until you noticed and reloaded by hand.</p>\n</blockquote>\n<p>Comparing a number on every greeting makes that self-correcting: <code>EventSource</code> reconnects on its own, the server greets it with the current revision, and if anything happened while it was away the page reloads. The revision is seeded from the clock so a restarted dev server always issues a higher number than the one it replaced, and bumped by at least one so two rebuilds inside the same millisecond are not read as &quot;nothing changed&quot;.</p>\n<p>Verified: with a page open, stop the dev server, edit a file, start it again — the page reloads itself as soon as the connection comes back.</p>\n<p>The other three events are ordinary notifications, since they only make sense while connected:</p>\n<table>\n<thead>\n<tr>\n<th>event</th>\n<th>data</th>\n<th>client</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>alive</code></td>\n<td>revision</td>\n<td>reload if higher than the one remembered</td>\n</tr>\n<tr>\n<td><code>css</code></td>\n<td>—</td>\n<td>swap <code>&lt;link&gt;</code> elements in place</td>\n</tr>\n<tr>\n<td><code>build-error</code></td>\n<td>compiler output</td>\n<td>show the overlay</td>\n</tr>\n<tr>\n<td><code>build-ok</code></td>\n<td>—</td>\n<td>clear the overlay</td>\n</tr>\n</tbody>\n</table>\n<p><code>build-error</code>, not <code>error</code>: <code>EventSource</code> dispatches its own connection failures under the name <code>error</code>, so the overlay would have appeared every time the stream hiccuped.</p>\n<h2>The routes widget</h2>\n<p>A draggable panel listing every route the application serves, read from the\ngenerated table — click one to navigate, <code>⌃R</code> to toggle, position and open state\nremembered across reloads. The collapsed pill is draggable too, with its own\nremembered position; four pixels of movement is the line between moving it and\npressing it, so a wobble during a click still opens the panel. A <code>.dyn</code> route gets a box for its parameter, since it\nhas no single URL to link to; <code>.client</code> routes are tagged <code>wasm</code>.</p>\n<p>It is part of the dev client served from <code>/_howl/alive.js</code>, so it exists only\nwhen <code>howl dev</code> is in front. A production build has neither the panel nor the\n<code>/_howl/routes.json</code> endpoint it reads.</p>\n<h2>Flags</h2>\n<pre><code>-dir      module root (default &quot;.&quot;)\n-addr     address to serve on (default &quot;:9000&quot;)\n-pages    page tree, relative to -dir (default &quot;client/pages&quot;)\n-static   static files, served from disk in dev (default &quot;client/public&quot;)\n-module   import path of -pages (default: derived from go.mod)\n-pkg      package to build (default &quot;.&quot;)\n-pre      shell command to run before generating routes\n-poll     filesystem poll interval (default 300ms)\n-args     arguments passed to the built binary\n</code></pre>\n<p><code>-module</code> is derived by finding the nearest <code>go.mod</code> above <code>-dir</code> and joining the path back down, so an app inside a larger module works without the flag.</p>\n<p><code>-pre</code> is for a generation step of your own. The documentation site uses it for its Markdown:</p>\n<pre><code class=\"language-bash\">howl dev -dir www -addr :9001 -pre &quot;go run github.com/mirairoad/howl-go/core/cmd/mddocs&quot;\n</code></pre>\n<h2>Why polling</h2>\n<p>Changes are detected by walking the tree every 300 ms and comparing modification time and size, not by <code>fsnotify</code>.</p>\n<p>Walking a few hundred files costs well under a millisecond, and it keeps the module's dependency count where it is. It also sidesteps the thing that makes filesystem events fiddly: most editors save by writing a temporary file and renaming it over the original, which arrives as <em>delete then create</em> rather than <em>write</em>, and needs special handling per platform. Raise <code>-poll</code> if the walk ever shows up in a profile.</p>\n<p>Generated files — <code>*_templ.go</code>, <code>fsroutes_gen.go</code>, <code>docs_gen.go</code>, <code>*.wasm</code> — are excluded from the watch. Without that, generating the route table changes a file, which triggers a rebuild, which generates the route table.</p>\n<p>A save that touches several files produces one rebuild: the watcher keeps re-scanning until the tree stops moving before it starts.</p>\n"

var _ = templruntime.GeneratedTemplate
