// Code generated by templ - DO NOT EDIT.

// templ: version: v0.3.1020
// Code generated by core/cmd/mddocs from the Markdown source. DO NOT EDIT.

package lifecycle

//lint:file-ignore SA4006 This context is only used if a nested component is present.

import "github.com/a-h/templ"
import templruntime "github.com/a-h/templ/runtime"

func Head() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var1 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var1 == nil {
			templ_7745c5c3_Var1 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 1, "<title>Lifecycle — howl-go</title><meta name=\"description\" content=\"templ has no lifecycle and cannot have one: a component renders once to a writer and is finished. There is no mount, no effect, no re-render. So &quot;do somethin…\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

func Page() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var2 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var2 == nil {
			templ_7745c5c3_Var2 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 2, "<article class=\"prose\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templ.Raw(body).Render(ctx, templ_7745c5c3_Buffer)
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 3, "</article>")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

const body = "<h1>Lifecycle</h1>\n<p>templ has no lifecycle and cannot have one: a component renders once to a writer and is finished. There is no mount, no effect, no re-render. So &quot;do something on first paint&quot; is a plain Go function in the page file.</p>\n<pre><code class=\"language-go\">func Mount()   { … }   // after this page's markup is in the DOM\nfunc Unmount() { … }   // optional: teardown that is not a registration\n</code></pre>\n<p>Both are reserved names, both optional, both run in the browser — on the cold load and again after every client-side navigation to that route.</p>\n<h2>Touching the DOM</h2>\n<p>Through <code>core/dom</code>, not <code>syscall/js</code>:</p>\n<pre><code class=\"language-go\">func Mount() {\n\troot := dom.Root()\n\tdom.Log(&quot;rows:&quot;, len(root.QueryAll(&quot;[data-rows] tr&quot;)))\n\n\troot.Delegate(&quot;click&quot;, &quot;[data-add]&quot;, func(dom.Event) {\n\t\tstore.Client().Add(&quot;from a Go click handler&quot;)\n\t})\n}\n</code></pre>\n<p>This matters more than it looks. A <code>.templ</code> file's generated Go is compiled for <strong>both</strong> targets, so importing <code>syscall/js</code> there would break the server build. <code>core/dom</code> splits the platform once — <code>dom_js.go</code> is real, <code>dom_stub.go</code> is no-ops — and the page stays neutral, needs no build tag, and compiles into the server binary as dead code that is never called.</p>\n<h2>Mount runs in a scope</h2>\n<p>Everything <code>Mount</code> registers — a <code>signal.Effect</code>, a <code>signal.Watch</code>, a <code>dom</code> listener — is released when the page is swapped out. The runtime opens a scope around <code>Mount</code> and disposes it before the next page's markup goes in. There is no stop func to keep, no release slice, and nothing to write in <code>Unmount</code> for them.</p>\n<p>Measured, on the example todos page: three away-and-back navigations then one mutation logs its watcher <strong>once</strong>. Before the scope existed the same page needed eleven lines of bookkeeping to get the same number, and forgetting one of them logged four times.</p>\n<p>The scope is lexical: it closes when <code>Mount</code> returns. A goroutine started in <code>Mount</code> — the hydrate fetch — finishes later, so anything it registers is outside the scope and lives for the tab. Goroutines write signals; the effects registered in <code>Mount</code> react. That is the shape hydration already has.</p>\n<p><code>Unmount</code> is for what is not a registration: a timer to cancel, a draft to save, a WebSocket to close.</p>\n<h2>Listeners: <code>On</code> and <code>Delegate</code></h2>\n<pre><code class=\"language-go\">root.Query(&quot;[data-form]&quot;).On(&quot;submit&quot;, func(e dom.Event) { add(e.Target().Query(&quot;input&quot;).Value()) })\nroot.Delegate(&quot;click&quot;, &quot;[data-del]&quot;, func(e dom.Event) { del(e.Target().Attr(&quot;data-del&quot;)) })\n</code></pre>\n<p><code>On</code> binds to one element. <code>Delegate</code> binds once to the root and matches every descendant that fits the selector — the ones in the markup now and the ones a later repaint renders. Inside a region a repaint replaces, <code>Delegate</code> is the only one that keeps working, because <code>Render</code> throws away the node an <code>On</code> was bound to. <code>howl check</code> warns on a listener bound per row inside a loop, which is the habit <code>Delegate</code> replaces.</p>\n<p>The handler receives the event: <code>Target()</code> is the matched element, <code>Value()</code> its value, <code>Key()</code> the key for a keyboard event, <code>PreventDefault()</code> when you need it. A <code>submit</code>, and a <code>click</code> that lands on a link, are prevented before the handler runs — the browser would otherwise navigate away mid-handler. Nothing else is: a click on a checkbox still toggles it, a keydown still types.</p>\n<p>Outside a scope — an app-shell control registered once at startup — both return the release func, and it is the only handle to the <code>js.Func</code> underneath. <code>dom.Off(release...)</code> takes several.</p>\n<h2>The repaint</h2>\n<pre><code class=\"language-go\">func repaint() {\n\titems := store.Todos.Get()\n\tdom.Root().Query(&quot;[data-list]&quot;).Render(ui.TodoList(items))\n}\n</code></pre>\n<p><code>Render</code> takes any templ component and draws it into the element — the same component the server rendered, rendered again in the browser. On an element the navigation has already thrown away it is a no-op, which is the ordinary end of an effect's life.</p>\n<p>The swap is <code>innerHTML</code>. It does not preserve focus, selection or scroll inside the region it replaces, so keep an input the user is typing into outside the element a repaint renders — the todos form is a sibling of the list, not a child.</p>\n<h2>Hydrating</h2>\n<p>A page with a store does not fetch its state: the server already rendered from it, so the page serialises that snapshot into its own markup and <code>Mount</code> reads it back.</p>\n<pre><code class=\"language-go\">// markup\n@templ.JSONScript(&quot;todos&quot;, store.SnapshotFrom(ctx))\n\n// Mount\nvar sn store.Snapshot\nif err := dom.Embedded(&quot;todos&quot;, &amp;sn); err == nil {\n\tstore.Client().Restore(sn) // publishes to the signal; the effect renders from it\n}\n</code></pre>\n<p>The fragment carries the script, so it works on a cold load and after a client-side navigation alike, and the browser store starts with exactly what is on screen. <code>howl check</code> warns on a <code>Restore</code> with no <code>Embedded</code>.</p>\n<h2>Fetching</h2>\n<p>Use <code>dom.GetJSON</code> / <code>dom.PostJSON</code>, or the generated typed client, for what the page does not already have — telling the server about a mutation, mostly. Both are the browser's own <code>fetch()</code>; <code>net/http</code> under wasm links a TLS stack the browser already has, and costs 2 MB gzipped for it.</p>\n<pre><code class=\"language-go\">go func() {\n\tif _, err := apiclient.New(&quot;&quot;).SyncTodos(context.Background(), []store.Op{op}); err != nil {\n\t\tdom.Warn(&quot;[todos] sync deferred:&quot;, err.Error())\n\t}\n}()\n</code></pre>\n<p>The goroutine is mandatory. Blocking the JS callback deadlocks the Go scheduler, because the fetch can only resolve once control returns to the event loop.</p>\n<h2>Per-frame animation</h2>\n<p><code>dom.Frame(func(t float64) bool)</code> runs once per paint until it returns false, and is released with the scope like a listener. Set width, transform or text directly from it; do not write a signal per frame, because an effect is a repaint and sixty repaints a second of a list is the cost this framework has no virtual DOM to absorb. The bar on <code>/lab</code> is eleven lines and moves the repaint counter by zero.</p>\n<h2>Why not <code>templ Mount()</code></h2>\n<p>A <code>templ</code> block compiles to a function that writes HTML. It has no way to express &quot;run this, return nothing&quot;. Hence the convention:</p>\n<blockquote>\n<p><strong><code>templ</code> for anything that produces markup, <code>func</code> for anything that does something.</strong></p>\n</blockquote>\n<p>Both live in the same page file.</p>\n"

var _ = templruntime.GeneratedTemplate
