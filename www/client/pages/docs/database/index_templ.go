// Code generated by templ - DO NOT EDIT.

// templ: version: v0.3.1020
// Code generated by core/cmd/mddocs from the Markdown source. DO NOT EDIT.

package database

//lint:file-ignore SA4006 This context is only used if a nested component is present.

import "github.com/a-h/templ"
import templruntime "github.com/a-h/templ/runtime"

func Head() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var1 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var1 == nil {
			templ_7745c5c3_Var1 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 1, "<title>The document store — howl-go</title><meta name=\"description\" content=\"db is optional and separate from the framework: a document store with an audit envelope, soft delete, optimistic locking and no migration framework. A collec…\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

func Page() templ.Component {
	return templruntime.GeneratedTemplate(func(templ_7745c5c3_Input templruntime.GeneratedComponentInput) (templ_7745c5c3_Err error) {
		templ_7745c5c3_W, ctx := templ_7745c5c3_Input.Writer, templ_7745c5c3_Input.Context
		if templ_7745c5c3_CtxErr := ctx.Err(); templ_7745c5c3_CtxErr != nil {
			return templ_7745c5c3_CtxErr
		}
		templ_7745c5c3_Buffer, templ_7745c5c3_IsBuffer := templruntime.GetBuffer(templ_7745c5c3_W)
		if !templ_7745c5c3_IsBuffer {
			defer func() {
				templ_7745c5c3_BufErr := templruntime.ReleaseBuffer(templ_7745c5c3_Buffer)
				if templ_7745c5c3_Err == nil {
					templ_7745c5c3_Err = templ_7745c5c3_BufErr
				}
			}()
		}
		ctx = templ.InitializeContext(ctx)
		templ_7745c5c3_Var2 := templ.GetChildren(ctx)
		if templ_7745c5c3_Var2 == nil {
			templ_7745c5c3_Var2 = templ.NopComponent
		}
		ctx = templ.ClearChildren(ctx)
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 2, "<article class=\"prose\">")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templ.Raw(body).Render(ctx, templ_7745c5c3_Buffer)
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		templ_7745c5c3_Err = templruntime.WriteString(templ_7745c5c3_Buffer, 3, "</article>")
		if templ_7745c5c3_Err != nil {
			return templ_7745c5c3_Err
		}
		return nil
	})
}

const body = "<h1>The document store</h1>\n<p><code>db</code> is optional and separate from the framework: a document store with an audit envelope, soft delete, optimistic locking and no migration framework. A collection is a Go struct.</p>\n<p>It is not an ORM. There are no relations, no query builder over tables, no code generation, and nothing in <code>core/</code> imports it. Reach for it when the alternative is hand-rolling <code>created_at</code>/<code>updated_at</code>/<code>deleted_at</code> and a migration directory for the fourth time; reach for <code>database/sql</code> when you want SQL.</p>\n<h2>A collection is a struct</h2>\n<pre><code class=\"language-go\">package users\n\nimport &quot;github.com/mirairoad/howl-go/db&quot;\n\ntype User struct {\n\tdb.Doc\n\tEmail string `json:&quot;email&quot;`\n\tName  string `json:&quot;name&quot;`\n\tPlan  string `json:&quot;plan&quot;`\n}\n\n// Optional. Runs on create, before validation — this is zod's .default().\nfunc (u *User) Defaults() {\n\tif u.Plan == &quot;&quot; {\n\t\tu.Plan = &quot;free&quot;\n\t}\n}\n\n// Optional. Runs on create and on every patch. A plain error becomes a 400.\nfunc (u *User) Validate() error {\n\tif !strings.Contains(u.Email, &quot;@&quot;) {\n\t\treturn errors.New(&quot;email: not an address&quot;)\n\t}\n\treturn nil\n}\n</code></pre>\n<p><code>db.Doc</code> is embedded, not wrapped, so the stored JSON stays flat — <code>{&quot;id&quot;:…,&quot;version&quot;:…,&quot;meta&quot;:{…},&quot;email&quot;:…}</code> — and the document can be returned straight out of an <code>api.Spec</code> response type.</p>\n<p>There is no schema library, for the same reason <code>core/api</code> has none: the struct is the schema. <code>encoding/json</code> enforces the shape, and the two things a validator adds beyond that — defaults and invariants — are those two methods. The TypeScript original needs zod because there the type is gone at run time.</p>\n<h2>Wiring the storage</h2>\n<pre><code class=\"language-go\">var Users = must(pg.New[User](context.Background(), conn, pg.Options{\n\tCollection: &quot;users&quot;,\n\tUnique:     []string{&quot;email&quot;},                    // unique generated column\n\tPromote:    []pg.Promote{{Path: &quot;plan&quot;}, {Path: &quot;seats&quot;, Type: pg.Bigint}},\n\tCache:      db.Cache{TTL: 5 * time.Minute},\n}))\n</code></pre>\n<p><code>conn</code> is anything satisfying <code>QueryContext</code>/<code>ExecContext</code> — <code>*sql.DB</code>, <code>*sql.Tx</code>, so pgx/stdlib or lib/pq, whichever the application already has. The package has no driver dependency of its own.</p>\n<p>Domain queries are methods on your package, not on the service:</p>\n<pre><code class=\"language-go\">func ByEmail(ctx context.Context, email string) (User, error) {\n\treturn Users.One(ctx, db.Query{Where: db.Eq(&quot;email&quot;, email)})\n}\n</code></pre>\n<h2>The surface</h2>\n<pre><code class=\"language-go\">u, err := Users.Create(ctx, User{Email: &quot;a@b.com&quot;, Name: &quot;Ada&quot;}, db.By(actor))\nu, err := Users.Get(ctx, id)                      // db.ErrNotFound when absent\nm, err := Users.GetMany(ctx, ids)                 // map[string]User\nxs, err := Users.Find(ctx, db.Query{\n\tWhere: db.And(db.Eq(&quot;plan&quot;, &quot;pro&quot;), db.Gte(&quot;seats&quot;, 2)),\n\tSort:  db.Desc(&quot;meta.created_at&quot;),\n\tLimit: 20,\n})\nu, err := Users.One(ctx, db.Query{Where: db.Eq(&quot;email&quot;, email)})\nn, err := Users.Count(ctx, db.Query{Where: db.Eq(&quot;plan&quot;, &quot;free&quot;)})\n\nu, err := Users.Patch(ctx, id, func(u *User) { u.Name = &quot;Ada L.&quot; }, db.By(actor))\nu, err := Users.PatchFields(ctx, id, db.Set{&quot;profile.plan&quot;: &quot;pro&quot;})\nerr     := Users.Delete(ctx, id, db.By(actor))    // soft; db.Hard() removes the row\nu, err  := Users.Restore(ctx, id)\n\nn, err := Users.PatchWhere(ctx, db.Eq(&quot;org_id&quot;, org), db.Set{&quot;plan&quot;: &quot;pro&quot;})\nn, err := Users.DeleteWhere(ctx, db.In(&quot;id&quot;, ids))\n</code></pre>\n<p>Options are variadic (<code>db.By</code>, <code>db.Hard()</code>, <code>db.Deleted()</code>, <code>db.Session(tx)</code>) for the operations addressed by an id, because <code>Get(ctx, id)</code> has to stay two arguments. Anything with a filter takes a <code>db.Query</code> struct instead, the same shape as <code>app.Config</code> and <code>api.Spec</code>.</p>\n<h3>Patch takes a closure</h3>\n<pre><code class=\"language-go\">u, err := Users.Patch(ctx, id, func(u *User) { u.Name = &quot;Ada L.&quot; })\n</code></pre>\n<p>Go has no <code>Partial&lt;T&gt;</code>, and this is better than one. The service has to read before it writes anyway — that is what validating the whole document means — so the caller may as well be handed the value it read. Field names are checked by the compiler, the write is locked to the version that was read, and a lost race is retried from the fresh document rather than by re-applying a stale delta. The closure may run more than once, so keep it a pure edit.</p>\n<p><code>PatchFields</code> is the same operation for when the field names are data rather than code: a form, a script, an admin tool. It reads, deep-sets the dotted paths, and validates the result exactly as <code>Patch</code> does.</p>\n<h2>The envelope</h2>\n<p>Every document carries one, and only the store writes it:</p>\n<table>\n<thead>\n<tr>\n<th>field</th>\n<th></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>id</code></td>\n<td>UUIDv7 — time-ordered, so inserts stay at the right edge of the primary key's B-tree instead of scattering across it</td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td>the optimistic lock. Starts at 1, increments on every patch, unchanged by a delete</td>\n</tr>\n<tr>\n<td><code>meta.created_at</code> / <code>created_by</code></td>\n<td>epoch milliseconds and the actor from <code>db.By</code></td>\n</tr>\n<tr>\n<td><code>meta.updated_at</code> / <code>updated_by</code></td>\n<td>stamped on every write</td>\n</tr>\n<tr>\n<td><code>meta.deleted_at</code> / <code>deleted_by</code></td>\n<td><code>nil</code> while the document is active</td>\n</tr>\n</tbody>\n</table>\n<p>Soft delete is the default. Every read carries <code>meta.deleted_at IS NULL</code>, which is index-backed because that path is a promoted column with a partial index on it. <code>db.Deleted()</code> lifts the filter; <code>db.Hard()</code> removes the row for good.</p>\n<h2>Filters</h2>\n<p>The grammar is the subset that compiles to an indexable SQL predicate: <code>$eq $ne $in $nin $gt $gte $lt $lte $or $and $exists</code>, plus dot-paths. Two spellings, one meaning:</p>\n<pre><code class=\"language-go\">db.And(db.Eq(&quot;plan&quot;, &quot;pro&quot;), db.Gt(&quot;seats&quot;, 10))\ndb.M{&quot;plan&quot;: &quot;pro&quot;, &quot;seats&quot;: db.M{&quot;$gt&quot;: 10}}\n</code></pre>\n<p>A comparison against <code>nil</code> matches a stored JSON null <strong>and</strong> an absent key — Mongo's rule, and the one soft delete is built on: a document written before the field existed has no such key at all.</p>\n<p>Aggregations, <code>$regex</code>, <code>$elemMatch</code> and text search are out. They need a query planner of their own; <code>SQL()</code> is the door, and everything through it is uncached and backend-specific.</p>\n<h2>Storage: JSONB plus promoted columns</h2>\n<pre><code class=\"language-sql\">CREATE TABLE &quot;users&quot; (\n  id   TEXT PRIMARY KEY,\n  doc  JSONB NOT NULL,\n  version    BIGINT GENERATED ALWAYS AS (((doc #&gt;&gt; '{version}'))::bigint) STORED,\n  deleted_at BIGINT GENERATED ALWAYS AS (((doc #&gt;&gt; '{meta,deleted_at}'))::bigint) STORED\n  -- one more per Promote entry\n)\n</code></pre>\n<p>Not pure-generic JSONB, because a JSONB path has no planner statistics and no range index. Not a column per field, because then every added field is a migration. The filter compiler routes a promoted path to its column and everything else to JSONB operators — a query is fast exactly where somebody said it needed to be, and correct everywhere else.</p>\n<p><strong>The table shape is fixed</strong>, so adding a field to the struct changes no DDL at all. The only thing that does is the <code>Promote</code> list, applied idempotently with <code>ADD COLUMN IF NOT EXISTS</code> at construction.</p>\n<p>Updates go through a recursive <code>howl_jsonb_deep_set</code>: plain <code>jsonb_set</code> does not create missing intermediate objects, so a patch to a path whose parent the stored document lacks would be silently dropped — a lost write, and one that only shows up for documents written before the field existed.</p>\n<h2>Evolving without migrations</h2>\n<p>Adding a Go field is not a schema change, but the documents written before it exists do not carry the key. <code>Report</code> is how you find out, and it is exact rather than sampled on any backend that can count JSON keys in one query:</p>\n<pre><code class=\"language-go\">report, _ := Users.Report(ctx)\nfor _, f := range report.Missing {   // declared, absent from some documents\n\tfmt.Println(f.Field, f.Docs, string(f.Default))\n}\nfor _, f := range report.Orphans {   // stored, no longer declared\n\tfmt.Println(f.Field, f.Docs)\n}\n\nUsers.Backfill(ctx, &quot;plan&quot;)          // writes the same default the report showed\nUsers.DropField(ctx, &quot;nickname&quot;)     // refuses the envelope and declared fields\n</code></pre>\n<p>Removing a path from <code>Promote</code> is additive in the other direction: the column and its index stay, maintained on every write and queried by nothing. <code>Columns</code> finds those orphans and <code>DropColumn</code> removes them; a still-declared column is refused, because the config is the source of truth.</p>\n<h2>In an endpoint</h2>\n<p>No glue — the document is already the response type:</p>\n<pre><code class=\"language-go\">// server/apis/users/id.dyn.api.go  -&gt;  GET /api/users/{id}\nvar Get = api.Define(api.Spec[api.None, api.None, users.User]{\n\tName: &quot;GetUser&quot;,\n\tHandler: func(r *api.Request[api.None, api.None]) (users.User, error) {\n\t\tu, err := users.Users.Get(r.Context(), r.Param(&quot;id&quot;))\n\t\tif errors.Is(err, db.ErrNotFound) {\n\t\t\treturn u, api.NotFound(&quot;no such user&quot;)\n\t\t}\n\t\treturn u, err\n\t},\n})\n</code></pre>\n<p><code>db.ErrConflict</code> is a 409 and <code>db.ErrInvalid</code> a 400; the wrapped message is your own validator's and is safe to show.</p>\n<h2>Caching</h2>\n<p>Off by default. <code>db.Cache{TTL: …}</code> turns on an in-process LRU, which is correct for one process and wrong the moment there are two — invalidation is per-process, so a second replica keeps serving its own copy. Supply a shared adapter for anything replicated.</p>\n<p>Keys are <code>&lt;prefix&gt;:&lt;collection&gt;:v&lt;version&gt;:get|find:…</code>. Invalidation moves the version, which makes every key built before it unreachable at once — no pattern scan, no key enumeration. Nothing is deleted; the LRU reclaims the orphans in its own time.</p>\n<p>Two things are never cached: anything carrying <code>db.Session(tx)</code>, because an uncommitted read must not be published and a write that may roll back must not evict; and a projected document under its by-id key, because half a document must not be served to a later <code>Get</code>.</p>\n<h2>Testing</h2>\n<p><code>db/memdb</code> is the same contract over a map: no persistence, no index, every query a scan.</p>\n<pre><code class=\"language-go\">users, _ := memdb.NewService[User](db.Options{Collection: &quot;users&quot;})\n</code></pre>\n<p>Both backends run the same suite in <code>db/conformance</code> — 24 cases, over 100 assertions. A contract described in prose drifts, because the first implementation defines what the words meant and the second implements what it read. The live Postgres run lives in <code>db/pg/livetest</code>, its own module so the driver it needs stays out of the framework's <code>go.mod</code>:</p>\n<pre><code class=\"language-sh\">docker run -d --name howl-conf-pg -p 54329:5432 \\\n  -e POSTGRES_PASSWORD=conf -e POSTGRES_DB=howl_conformance postgres:16-alpine\nmake test-db-pg\n</code></pre>\n<h2>Tooling</h2>\n<p><code>howl check</code> carries two rules for the store, both for mistakes that fail silently rather than loudly:</p>\n<table>\n<thead>\n<tr>\n<th>rule</th>\n<th></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>page-imports-db</code></td>\n<td>a page importing <code>db</code> — the same rule as <code>core/app</code>. The page tree has to stay leaves, and <code>db</code> links <code>database/sql</code> into the wasm build. Load documents in an endpoint or <code>Config.Data</code></td>\n</tr>\n<tr>\n<td><code>collection-value-receiver</code></td>\n<td><code>Defaults</code> on a value receiver. It still satisfies the interface, so the service calls it — and every default is written to a copy. Nothing fails; the field is empty forever</td>\n</tr>\n</tbody>\n</table>\n<p><code>howl_scaffold</code> takes <code>kind: &quot;collection&quot;</code>:</p>\n<pre><code class=\"language-json\">{&quot;kind&quot;: &quot;collection&quot;, &quot;name&quot;: &quot;users&quot;, &quot;fields&quot;: [&quot;email:string&quot;, &quot;seats:int64&quot;]}\n</code></pre>\n<p>It writes <code>server/store/users.go</code> with the envelope embedded, the pointer receiver correct, and the <code>Unique</code>/<code>Promote</code> lines commented in place. Unlike a page or an endpoint the location carries no machine meaning — nothing generates from that directory, so move the file wherever it belongs.</p>\n<h2>Interoperating with the TypeScript services</h2>\n<p>The stored shape matches <code>@hushkey/service-core</code> key for key: the same envelope names, the same epoch-millisecond timestamps, the same generated-column definitions, the same <code>sql</code> cache namespace. A Go service and a Deno service can drive the same table, which is what makes porting one endpoint at a time possible.</p>\n<h2>What is deliberately missing</h2>\n<ul>\n<li><strong>Projection returns a partial struct</strong> — it cannot. An omitted field comes back as its zero value, indistinguishable from a stored empty one. Use <code>Project</code> to save bandwidth, not to signal absence.</li>\n<li><strong>Transactions are not wrapped.</strong> Run <code>BEGIN</code>/<code>COMMIT</code> on a <code>*sql.Tx</code> and pass it as <code>db.Session(tx)</code>; caching is skipped for anything that carries one.</li>\n<li><strong>Bulk writes do not validate.</strong> <code>PatchWhere</code> writes the same fixed values to every match with no per-row read — that is the point of it. For anything that has to be checked per document, loop over <code>Patch</code>.</li>\n<li><strong>Roles, ownership and permissions.</strong> Same answer as <code>core/api</code>: the store has no user model, and a framework that guesses at one is a framework you have to fight.</li>\n</ul>\n"

var _ = templruntime.GeneratedTemplate
