package dashboard

import (
	"howl-go/client/store"
	"howl-go/client/ui"
)


templ Page() {
	<div class="row">
		<h2>Overview</h2>
		<span class="pill cold">same component · server, static or wasm</span>
	</div>
	@ui.StatCards(store.MetricsFrom(ctx).Cards)
}
